[project]
name = "linkedin-agent"
version = "0.1.0"
description = "AI LinkedIn Post Generator using LangGraph"
authors = [{ name="Your Name" }]
requires-python = ">=3.10"


dependencies = []

[tool.setuptools.packages.find]
where = ["src"]